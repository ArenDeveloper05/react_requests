import Counter from "../counter/Counter";
import "./Header.scss";

const Header = () => {
  return (
    <header>
      <h1>Header</h1>
      <Counter />
    </header>
  );
};

export default Header;
